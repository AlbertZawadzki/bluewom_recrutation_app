when:
  event: manual
  branch: master

depends_on: [ 'clone' ]

steps:
  setup:
    image: alpine
    environment:
      ENV_VARS:
        from_secret: ENV_VARS
    commands:
      - echo "$ENV_VARS" > .env.prod