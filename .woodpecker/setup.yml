when:
  event: manual
  branch: master

steps:
  setup:
    image: alpine
    environment:
      ENV_VARS:
        from_secret: ENV_VARS
    commands:
      - echo "$ENV_VARS" > .env.prod

depends_on:
  - clone